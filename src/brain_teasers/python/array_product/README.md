# create a function array_product that accepts an array
# if the array is empty, return an empty array
# if the array is a unit, return the unit
# else return the cummulative product using recursive function